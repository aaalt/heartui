
ext	V free_array(S* array, I len);

ext V make_arrays();

ext C blstr_mask(C blink, C str);

ext C get_blink(C ch);
ext	C get_str(C ch);
ext	V make_struct_nmb(numbers par, I len);


ext	V num_to_back(numbers par, background back, S* col_palette, S* blink_st_palette, S* blink_col_palette, S* str_st_palette, S* str_col_palette);